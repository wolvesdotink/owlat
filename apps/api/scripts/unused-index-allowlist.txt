# Convex indexes that check-unused-indexes.sh may find unqueried.
#
# Format: `<table>.<index>` per line. An entry that stops being unused — because
# something now queries it, or because the index was deleted — fails the gate,
# so this file cannot rot into a list of permanent exemptions.
#
# Keep the reason with the entry.

# Lands with the IMAP CONDSTORE (RFC 7162) work: `SEARCH MODSEQ` / `CHANGEDSINCE`
# needs a per-folder modseq range, and dropping the index now would only mean
# re-adding it (and re-backfilling) in that change.
mailMessages.by_folder_and_modseq
