{
	"name": "@owlat/ostr-observer",
	"version": "0.4.4",
	"private": true,
	"description": "OSTR observer-side machinery: DKIM evidence-bundle capture (exact h= header retention), key-observation emission, windowed aggregation with k-anonymity bucketing, spam-report batch commitments, and attestation submission. Strictly opt-in; hard-disabled below the small-instance mailbox threshold (plan §7.4).",
	"homepage": "https://owlat.app",
	"license": "Apache-2.0",
	"type": "module",
	"main": "./src/index.ts",
	"module": "./src/index.ts",
	"types": "./src/index.ts",
	"exports": {
		".": {
			"types": "./src/index.ts",
			"import": "./src/index.ts"
		}
	},
	"scripts": {
		"lint": "oxlint --config ../../oxlintrc.json src",
		"lint:fix": "oxlint --config ../../oxlintrc.json src --fix",
		"typecheck": "tsc --noEmit",
		"test": "vitest run --passWithNoTests",
		"test:watch": "vitest",
		"test:coverage": "vitest run --coverage --passWithNoTests"
	},
	"dependencies": {
		"@owlat/ostr-core": "workspace:*"
	},
	"devDependencies": {
		"@types/node": "catalog:",
		"@vitest/coverage-v8": "catalog:",
		"typescript": "catalog:",
		"vitest": "catalog:"
	}
}
