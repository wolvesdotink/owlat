/**
 * Shared constants for the email builder package.
 */

// Email layout
export const BASE_CONTAINER_WIDTH = 600;

// Image upload constraints
export const MAX_IMAGE_SIZE_BYTES = 5 * 1024 * 1024; // 5MB

// History management
export const MAX_HISTORY_ENTRIES = 50;
export const HISTORY_DEBOUNCE_MS = 300;
export const HISTORY_CHECKPOINT_INTERVAL = 10;
export const MAX_HISTORY_CACHE_SIZE = 10;

// UI positioning
export const BUBBLE_MENU_GAP = 8;
export const VIEWPORT_PADDING = 16;
export const DEFAULT_MENU_HEIGHT = 48;

// Timeouts
export const FOCUS_MODE_HINT_TIMEOUT_MS = 2500;
export const TOOLTIP_HIDE_TIMEOUT_MS = 2000;

// Color picker
export const MAX_RECENT_COLORS = 8;
