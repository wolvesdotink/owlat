/**
 * Shared types for control components
 *
 * Re-exports block types with control-friendly aliases to avoid
 * duplicating interface definitions across control components.
 */

export type {
	UniversalPadding as Padding,
	UniversalMargin as Margin,
	UniversalBorder as Border,
	BorderStyle,
} from './blocks';
