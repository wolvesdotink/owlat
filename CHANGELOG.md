# Changelog

All notable changes to Owlat are documented here. Format follows [Keep a Changelog](https://keepachangelog.com/en/1.1.0/), and the project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

Release sections are generated by `bun run release:cut <version>` (see `scripts/release.ts`) from conventional-commit subjects since the previous tag, folding in whatever was hand-curated under [Unreleased]. Notable changes can still be written under [Unreleased] as they land.

## [Unreleased]

<!-- Add released versions below this line, newest first. -->

## [0.3.0] - 2026-07-16

### Added
- **Sealed Mail**: end-to-end encryption for personal mail between Owlat instances (PGP/MIME, RFC 9580 via openpgp.js). Per-address keys are generated and sealed at rest, published over WKD and a signed `/.well-known/owlat.json` instance manifest, discovered with TOFU fingerprint pinning and Signal-style key-change alerts. Mail auto-seals when every recipient has a usable key; the reader shows a "Sealed" badge and decrypt-on-ingest keeps search and the AI agent working. Key rotation, revocation, and a recovery kit (no admin escrow) are included. Postbox 1:1 plane only.
- **Sender authenticity**: honest SPF/DKIM/DMARC badges on personal mail, including a "not authorized to send for this domain" state; inbound ARC verification rescues legitimately-forwarded mail; sender-impersonation heuristics (look-alike domains, reply-to mismatch).
- **Transport security**: publish our own MTA-STS policy, DANE (RFC 7672) at send time via `DANE_MODE` (`off`/`report`/`enforce`, default report-only — evaluate and report TLSA in TLS-RPT without ever bouncing mail), and a TLS-RPT ingest dashboard. Outbound TLS posture is selectable (`OUTBOUND_TLS_MODE`).
- **At rest**: all message bodies and stored `.eml` blobs are sealed with an instance data key; a fail-closed decrypt-serving proxy streams raw bytes on demand. DKIM private keys and SMTP relay credentials are sealed at rest (`MTA_SECRET`).

### Changed
- **api**: message-body reads go through a single `getMessageBody()` accessor (CI-ratcheted), enabling uniform at-rest sealing.

### Security
- No query or action path returns private key material; WKD/manifest routes expose public material only; discovery fetches are SSRF-hardened; absent authentication verdicts render as "unknown", never "verified".

### Added
- mark alpha status with a badge on website, app, and README
- **mta**: validate DANE-TA and isolate pooled policy
- **mta**: authenticate DANE-EE before SMTP resumes
- **mta**: add DNSSEC-aware DANE destination discovery
- **mail**: require TLS for inbound delivery
- **mta**: add DANE report-only mode (DANE_MODE off/report/enforce)
- **setup-cli**: seal the relay password in the `env` setter's .env backup
- default Sealed Mail and sender-authenticity flags ON
- **api**: sealed at rest — seal all message bodies with an instance data key (Sealed Mail E8b) (#336)
- **api**: key lifecycle — rotation, revocation, recovery kit (Sealed Mail E6) (#337)
- **web**: trust surfaces — composer lock, sealed badges, key-change banner (Sealed Mail E5) (#334)
- **api**: inbound unsealing + signature verification (decrypt-on-ingest) — Sealed Mail E4 (#332)
- **api**: outbound sealing — PGP/MIME with protected headers (flag sealedMail) (#331)
- **api**: recipient key discovery + TOFU pinning (#330)
- **api**: E2EE key vault + WKD publication + signed instance manifest (flag sealedMail) (#326)
- **mta**: DANE at send time (flag DANE_ENABLED) (#329)
- outbound DMARC-alignment guard in delivery readiness + From pickers (#325)
- inbound ARC verification with trusted-forwarder overrides (Sealed Mail A5) (#328)
- publish our own MTA-STS policy (RFC 8461) (#323)
- TLS-RPT inbound ingestion + delivery-page dashboard (#324)
- sender-impersonation heuristics (scanner rule + ingest-side flags) — Sealed Mail A4 (#322)
- **web**: sender-authentication badge + reply guard (flag senderAuthBadges) (#321)
- **mta**: seal DKIM keys + relay credentials at rest (#317)
- **mta**: outbound TLS policy switch + relay TLS floor (#319)
- **api**: thread auth verdicts into the reader queries (Sealed Mail A2) (#320)
- persist SPF/DKIM/DMARC verdicts on both inbound paths (Sealed Mail A1) (#318)
- Sealed Mail P0 scaffolding — CI triggers, openpgp, flags, createSecretBox, fixtures (#316)
- **ui**: type-aware toast lifetimes + info/warning types + assertive error announcements (#304)
- **web**: make receiving (MX) setup visible before inbound is enabled (#299)
- **web**: wire useUnsavedChanges into the editors that silently discard work (#298)
- **web**: content-shaped skeletons instead of spinners on major surfaces (#279)
- **web**: an honest first send — never complete it without a transport (#295)
- **inbox**: optimistic triage + undo for the agent inbox (Postbox parity) (#296)
- **web**: one delivery-readiness surface (transport + domain + DKIM/SPF/MX, one truth) (#280)
- **setup**: persist wizard progress so a refresh does not wipe it (#292)
- **desktop**: native file dialogs + OS file-drop for uploads (#290)
- smooth invites on a brand-new instance (pre-domain-verification) (#291)
- **inbox**: sender-trust gate + code-agent injection check for inbound feature requests (#272)
- **web**: collapse stacked onboarding surfaces into one adaptive Getting started (#289)
- **web**: branded SPA cold-boot splash instead of a blank window (#288)
- Azure adapter + live model picker + per-provider spend labels + Connect-your-AI docs (#286)
- first-login "Connect your AI" step + treat a stored key as satisfying the ai flag (#285)
- **web**: AI provider settings page (provider/model/key/base-url/embeddings + Test connection) (#284)
- **api**: local-by-default embedding plane (+ optional hosted override, dimension guard) (#283)
- **api**: aiProviderConfig table + encrypted key store + save/get/test functions (#281)
- **setup**: one-time token gate on the unauthenticated setup endpoints (#269)
- **web**: in-app transport editor (change sending provider without the CLI) (#256)
- **api**: Google + OpenRouter language adapters (with OpenRouter listModels) (#260)
- **web**: a real empty-state system across primary list surfaces (#251)
- **web**: backups in the dashboard — status, schedule, run-now (#255)
- **web**: right-click context menus, app-wide (#257)
- provision a mailbox straight from the request (close the loop) (#253)
- **web**: a graceful post-setup restart handoff (no 24s apparent hang) (#250)
- **web**: optimistic mutations beyond Postbox (generalize the pattern) (#252)
- turn the non-invited dead-end into a door (request access) (#258)
- **web**: page transitions + a route-progress indicator (#254)
- **api**: native Anthropic language adapter (@ai-sdk/anthropic) + pricing rows (#248)
- **web**: rename Organization → Workspace + connected-workspaces manager in settings (#249)
- "Move my mailbox here" — staged full move with MX guidance (#246)
- post-import "switch your sending" prompt (outbound-only, gated) (#245)
- **web**: move Audience below Send in the sidebar (#244)
- **web**: the fresh-start path — the default welcome (#243)
- **desktop**: draw the workspace accent frame in the native macOS window (#229)
- send-as choice — reply as the team or as yourself (#242)
- open campaigns to editors (guardrail now exists) (#241)
- **web**: one import wizard for every provider (Gmail + generic IMAP unified) (#240)
- invite someone straight into a team inbox (#237)
- **web**: team inboxes in the Postbox — sidebar, shared read state, stale-reply guard (#239)
- **web**: campaign wizard sender picker replaces free-text from-fields (#238)
- **web**: first-login welcome flow + resumable per-user checklist (#236)
- **web**: one Sending surface — transport card + guided switch (#235)
- **web**: SMTP relay in the setup wizard with provider presets (#233)
- create a team inbox + manage its members (#232)
- **web**: team page redesign — members table, roles at point of choice (#231)
- **web**: admin "Campaign senders" settings page + custom-sender toggle (#230)
- **api**: shared-mailbox membership model (mailboxMembers + access choke point) (#221)
- **api**: generic SMTP relay send-provider adapter (#227)
- **web**: invite flow — copyable link, resend, mailbox reserved by default (#224)
- **api**: campaignSenders table + send-time enforcement (#226)
- **desktop**: a titlebar that earns its pixels (#217)
- **delivery**: the health hub — verdict, trends, domains, thresholds (#212)
- **campaigns**: report redesign on the chart kit + A/B fold-in (#228)
- **campaigns**: one command center replaces index/all/reports (#209)
- **api**: SES bounce & complaint feedback loop via SNS (#225)
- **api**: per-user onboarding state (userOnboarding table) (#220)
- instance-level migrationMode flag (default off) (#222)
- **audience**: Suppressions under Audience + inline "why no mail?" answer (#213)
- **desktop**: reply-from-notification, compose quick action, compose window memory + spring (#219)
- **desktop**: workspace switching — visible, instant-feeling (#218)
- **desktop**: macOS traffic lights follow the sidebar (#216)
- **web**: sidebar hide (Cmd-\) + hover-peek overlay (#215)
- **desktop**: per-workspace accent frame + derived tints (#214)
- **desktop**: redesign app icon with brand background (#210)
- **api**: add AI-related documentation and configuration
- **web**: global Cmd-K across email objects (#208)
- **inbox**: assignment people notice — avatar picker, assign-to-me, notify (#206)
- **inbox**: filter pills, needs-attention sort, URL state for Team Inbox (#205)
- **inbox**: Team Inbox rows on Postbox row DNA + per-user unread (#200)
- **inbox**: actionable All-activity feed + guided empty state (#202)
- **inbox**: soft-hold on send/approve while a teammate is replying (#198)
- **inbox**: thread presence — who is viewing, who is replying (#197)
- **inbox**: unified status chip + team snooze (#196)
- **postbox**: resizable composer popups + dock + focus compose (#195)
- **web**: auto-advance task flow for both agent queues (#191)
- **marketing**: re-add bento grid micro-animations (#192)
- **postbox**: Daily Brief card (#190)
- **postbox**: centered reader overlay + keyboard flow for the Today view (#189)
- **postbox**: Today view — the focused default inbox landing surface (#188)
- **web**: human trust language for agent confidence + self-check flags (#186)
- **postbox**: labeled segmented view-mode control with server persistence (#184)
- **ui**: FF chart kit — UiTrendChart, UiSparkline, UiStatTile, UiBars (#182)
- **ui**: micro-interaction kit — hover-reveal, spring press, stagger, ticker (#180)

### Changed
- **api**: format sealed body tests
- **postbox**: extract composer drop zone
- **mail**: split draft query handlers
- **api**: name indexes after indexed fields
- **e2ee**: prefix persisted boolean fields
- drop the literal PGP header from recovery-kit copy and doc comment
- **api**: getMessageBody() accessor over all message-body reads + CI ratchet (#327)
- **api**: adopt getOrThrow in mergeContacts load-or-404 (#306)
- **web**: split the three oversized SFCs (no behavior change) (#303)
- **ui**: consolidate the component kit (shared UiSpinner, UiModal conversion, z-index + label tokens) (#301)
- **web**: useRouteId helper for branded route-param ids (#302)
- **web**: consolidate status-badge styling on semantic FF tokens (#300)
- **web**: route list/detail reads through UiQueryBoundary (stop errors reading as empty) (#293)
- **marketing**: adopt the shared button kit + type scale (#278)
- **api**: adopt getOrThrow for hand-rolled load-or-404 sites (#294)
- **web**: remove avoidable unsafe casts and non-null-assertion clusters (#277)
- **web**: shared capitalize, personInitials, SendStatusBadge, webhook status map (#276)
- **api**: unify email normalization on @owlat/shared normalizeEmail (#271)
- **api**: resolveAiConfig(ctx) + rename getLLMProvider → resolveLanguageModel across all call sites (#282)
- **api**: rename convex organizations module → workspaces (#259)
- sweep Fowler code smells across the monorepo (#266)
- **web**: extract useAutomationBadges (dedupe automation badge helpers) (#268)
- kill full-reload navigation (menu, notification-open) (#262)
- **docs**: repair the illustration layer's token discipline (#263)
- **ui**: named FF type scale + sweep settings system/operator (#261)
- **api**: LLM provider-adapter registry (openai + openai-compatible), resolve env through it (#247)
- **desktop**: remove the menu-bar tray icon — close means quit, dock badge for unread (#223)
- **campaigns**: 3-step wizard — Setup, Content, Review (A/B opt-in) (#207)
- **web**: IA — first-class Delivery section with live health dot (#204)
- **web**: IA — the Send section (campaigns, automations, transactional, templates) (#203)
- **convex**: reduce unbounded .collect()/.filter() and make the gate honest (#199)
- **postbox**: one AI home in the thread reader (#194)
- **web**: shared AgentTaskCard system for Reply Queue + Review Queue (#187)
- **web**: one meaning per word — Team Inbox, All activity, subscription topics (#183)

### Fixed
- **security**: fail closed without blob proxy origin
- **security**: bind DNS checks and cap DoH streams
- **tls-rpt**: harden ingestion and reporter semantics
- **ci**: support macOS bash in format gate
- **api**: standardize new HTTP error responses
- **api**: version stored JSON encodings
- **settings**: audit organization configuration changes
- **api**: cap persisted user-sized arrays
- **api**: bound new Convex query scans
- **mail**: normalize inbound sender addresses
- **e2ee**: keep recovery imports decrypt-only
- **e2ee**: make key lifecycle writes atomic
- **api**: authenticate blobs before migration skip
- **api**: seal conversation previews at rest
- **e2ee**: withdraw public keys when disabled
- **postbox**: require explicit unsealed send consent
- **e2ee**: preserve PGP MIME on first-contact sends
- **mta**: default DANE mode to off
- **api**: mint GnuPG-compatible OpenPGP keys (legacy curve25519 profile) (#333)
- **web**: wire room selection and create-dialog navigation on chat index page (#312)
- **web**: collapse duplicated Postbox navigation into a single sidebar link (#310)
- **desktop**: stop registering Cmd/Ctrl+K as a system-wide shortcut (#308)
- **ui**: keep content clear of the fixed desktop titlebar (#309)
- **web**: make interactive rows keyboard-operable and labelled (#297)
- **web**: confirm irreversible one-click destructive actions (#275)
- **web**: build requireAdmin token URL from trusted config; document internal routes (#267)
- **api**: hardening bundle — multipart cap, rate-limit warn, audit attribution, api-key expiry (#273)
- **web**: stop auth proxy trusting client X-Forwarded-For (rate-limit bypass) (#264)
- **api**: mark the SMTP send-provider chain as 'use node' (#287)
- **web**: collapse the stacked onboarding checklists into one gated flow (#274)
- **web**: verify the compose template on self-update (parity with system/update) (#270)
- **web**: sanitize user-authored HTML before v-html in snippets & signatures (#265)
- **web**: keep campaign wizard KeepAlive slot clean

### Documentation
- **mta**: document DANE_MODE (off/report/enforce, default report)
- register Sealed Mail guide and transport security pages in the sidebar
- add Sealed Mail user guide, recovery kit, and transport security pages
- changelog entry for the Sealed Mail release
- **web**: attribute cookie forwarding to authedConvexClient in token-url note (#305)
- progressive-disclosure standard + PR checklist item (#181)

## [0.2.1] - 2026-07-06

### Added
- **web**: manual connection-code fallback for the desktop sign-in handshake
- opt-in 'bun run dev:seed' to populate the local dev stack
- **api**: seed dummy teammate accounts in the demo seed
- **desktop**: sign updater artifacts in release builds

### Changed
- **web,ui**: dedupe auto-imported helper exports (#178)

### Fixed
- **web,desktop**: allow loopback-IP Convex backends in the desktop CSP
- **web,api**: stop BetterAuth's 'Rate limiting skipped' warning
- **web**: exempt the BetterAuth proxy from nuxt-csurf
- **api**: read BETTER_AUTH_SECRET lazily so pushes survive component schema derivation (#177)

## [0.2.0] - 2026-07-06

### Changed

- **Self-hosted-first positioning.** The landing page, docs, and compose stack now treat self-hosting as the primary deployment mode. Hosted cloud is moved to a waitlist (`/waitlist`) and is coming later.
- `docker-compose.yml` now references `ghcr.io/wolvesdotink/*` images with a `${OWLAT_VERSION:-latest}` tag, falling back to local `build:` for dev. The hosted-cloud control plane has moved to a separate private repository — this repo is OSS-only.
- `docker-compose.yml` adds a new `caddy` service under `--profile tls` that fronts `web`, `convex`, and `mta` with automatic HTTPS.

### Added

- **One-liner installer.** `curl -fsSL https://get.owlat.app | bash` clones the repo, verifies prerequisites, installs the `owlat` CLI, and launches the setup wizard. Supports `OWLAT_ASSUME_YES=1` and `OWLAT_CONFIG_FILE` for non-interactive / CI use.
- **`owlat` CLI wrapper** (`/usr/local/bin/owlat`). Hides `docker compose` ceremony: `owlat start|stop|status|logs|doctor|backup|restore|upgrade|version`.
- **In-app updates.** Platform admins see **Settings → System &amp; Updates** with current version, container health, an "Update available" card, and per-run history. Backed by a new `systemUpdates` Convex table and `POST /api/system/update` Nitro route that drives the existing updater sidecar with a pinned compose template.
- **Updater sidecar wired into `docker-compose.yml`.** Mounts the host docker socket and the install dir; exposes `/update` and `/health` authenticated by `INSTANCE_SECRET`.
- **`scripts/setup.sh doctor`** subcommand diagnoses the most common self-host failure modes: Docker presence, SMTP port 25 egress, DKIM key format, secret generation, container state, EHLO DNS.
- **`scripts/backup.sh` + `scripts/restore.sh`**: volume-level backup/restore via short-lived busybox containers. Archives `convex-data`, `redis-data`, `clamav-data`, and `.env`. SHA256-checksummed.
- **Release pipeline** (`.github/workflows/release.yml`): tag-triggered build/push of every service image to GHCR (`:<semver>` + `:latest`), auto-generates `docker-compose-<version>.yml` via `scripts/gen-release-compose.sh`, and creates a GitHub Release with the compose file attached.
- All Dockerfiles now accept `OWLAT_VERSION` / `OWLAT_GIT_SHA` / `OWLAT_BUILD_DATE` build args, set them as `ENV`, and apply OCI image labels (`org.opencontainers.image.*`).
- **Self-host onboarding banner** (`apps/web/app/components/dashboard/SelfHostOnboardingBanner.vue`): surfaces three next steps (verify domain, open Convex dashboard, read docs) on first login, auto-hides once a domain is verified, dismissible per-user via localStorage.
- **Hosted-cloud waitlist** (`apps/marketing/app/pages/waitlist.vue`): minimal email-capture form submitting to `NUXT_PUBLIC_WAITLIST_ENDPOINT`.
- New Caddy example config (`Caddyfile.example`) that terminates TLS and fronts web/convex/mta subdomains.

### Documentation

- `apps/docs/content/1.guide/1.getting-started.md`: prepended "Deploy Owlat" section with the one-liner install.
- `apps/docs/content/3.developer/30.self-hosting.md`: added "Fastest path" callout and clarified which control-plane features are self-hosted vs hosted-only.
- `apps/docs/content/3.developer/8.environment-variables.md`: split into "Self-hosted configuration" (primary) and "Hosted-cloud operators" (callout-gated) sections.
- `apps/docs/content/3.developer/34.self-hosting-maintenance.md`: documented the three update paths (in-app / CLI / manual) and added a "Recovering from a failed update" section with diagnosis steps and rollback instructions.
- `README.md`: rewritten with self-host-first framing, one-liner install, and resource requirements.

### Added
- **web,api**: natural-language handling rules (#168)
- **api**: real free/busy availability grounding for scheduling replies (#166)
- **api**: edit-learning flywheel — diff draft→sent into the voice profile + per-contact overrides (#163)
- **web,api**: autonomy trust controls — graduation nudge, kill switch, working hours (#161)
- **web,api**: bring on-arrival draft + confidence + review gate into personal Postbox (#167)
- **web,api**: Daily Brief digest + bidirectional commitment/deadline tracking (#165)
- **api**: quality/eval harness — golden set, edit-distance metric, ask-rate dashboards (#164)
- **ui**: adopt Fluid Functionalism design system (#162)
- **api**: per-sender/per-contact autonomy with a first-N-observed warm-up (#160)
- **api**: inbound injection hardening -- quarantined extraction + hidden-text strip + guard beyond 8k (#158)
- **web,api**: AI draft proposes/attaches the right file (#157)
- **web,api**: freeform revise-by-instruction on the whole draft + streaming (#156)
- **api**: preserve grounding under compaction + agentic fetch-more in the draft path (#155)
- **api**: durable grounding — open-commitments recall + curated policy/FAQ with precedence (#154)
- **api**: answer-memory — persist clarification answers so it never asks twice (#153)
- **web,api**: ask-eagerness dial + per-email cap + ask→answer→draft-delta instrumentation (#152)
- **web**: "Coach my draft" rung for high-stakes mail (#151)
- **api**: prompt caching + complexity routing in the drafting path (#150)
- **web,api**: strip tracking pixels / remote images on inbound mail (#149)
- **web,api**: smart snooze — AI-suggested wake time + snooze-until-they-reply (#148)
- **web,api**: VIP/important-sender signal + unified cross-thread priority score (#147)
- **api**: capture post-send outcomes (replies/bounces/complaints) as autonomy feedback (#146)
- **web,api**: Postbox-native clarify — "Needs your input" Reply Queue card (#129)
- **api**: per-org dollar spend budget that fails closed (#144)
- **web,api**: offer 2-3 draft options at the review gate (#139)
- **api**: shadow "would-have-sent" mode + per-category/sender scorecard (#135)
- **api**: clarify step — slot-based missing-info detection with divergence gating (#126)
- **api**: awaiting_clarification lifecycle state + pendingClarification + resume-into-draft (#121)
- **api**: configurable send-delay / undo window on autonomous sends (#106)
- **web**: before/after diff when reviewing or editing an agent draft (#116)
- **web**: keyboard-first agent review queue (j/k/Enter + approve/edit/reject) (#114)
- **web,api**: persist and surface the autonomy decision reason + draft grounding (#110)
- **api**: never widen autonomy without the user — graduation-suggestion gate (#103)
- **api**: server-derived recipient + deterministic pre-send reference monitor on auto-send (#100)
- **api**: draft-quality self-check that gates autonomy on the draft, not the classifier (#97)
- **web,api**: text snippets with slash-trigger in the composer (#76)
- **api**: emit a retrieval coverage/confidence signal from context_retrieval (#95)
- **api**: apply the learned writing-voice profile to the autonomous agent draft (#94)
- **web**: offline read cache — instant cold start + readable mail without a connection (#93)
- **desktop,web**: richer notifications — smart-category rules, grouping, tray peek (#91)
- **desktop,web**: pure mailto parser + default-mail-app setup for the desktop shell (#90)
- **web**: Postbox render cache, iframe pre-sizing, composer chunk warmup (#89)
- **web**: windowed thread list + cursor pagination for large folders (#88)
- **web**: optional Postbox send sound (off by default) (#87)
- **web**: comfortable/compact density + icon-collapsible folder rail (#86)
- **web**: one consistent motion system for Postbox transitions (#85)
- **web**: progressive disclosure — reader actions behind hover/⋯, leaner compose footer (#84)
- **web**: :shortcode: emoji picker in the composer (#83)
- **web**: default reply-all vs reply preference + visible compose toggle (#82)
- **web**: attachment chips — upload progress, thumbnails, size meter (#81)
- **web,api**: paste/drop images inline into the Simple composer (cid embedding) (#80)
- **web,api**: smarter recipient field — recency ranking, external-domain cue, reply-all gap hint (#79)
- **web**: hide the composer toolbar until text is selected (#78)
- **web**: markdown-style typing shortcuts in the Postbox composer (#77)
- **web,api**: detect scheduling requests and pre-draft an availability reply (#75)
- **web,api**: "Ask about this thread…" — assistant scoped to the open conversation (#74)
- **web,api**: automatic cached summary strip on long threads (#73)
- **web,api**: selection AI pill — shorter · friendlier · formal · fix grammar · translate (#72)
- **web,api**: inline ghost-text autocomplete in the composer (Tab to accept) (#71)
- **api**: per-identity writing-voice profile learned from sent mail (#70)
- **web,api**: smart inbox sections — People, Newsletters, Notifications, Receipts (#69)
- **web,api**: "remind me if no reply" follow-up tracking on sent mail (#68)
- **web**: the Reply Queue — an AI task list of emails waiting on you (#67)
- **api**: detect and track "needs a reply from me" on personal mail (#65)
- **web**: honest link destinations in received mail (tooltip + mismatch flag) (#62)
- **web**: inline reply box pinned under the conversation (#60)
- **web**: reader typography + header polish (measure cap, timestamps, avatars) (#57)
- **web,api**: one-click Unsubscribe chip from List-Unsubscribe headers (#53)
- **web**: tracking-pixel detection + privacy badge on received mail (#50)
- **web**: in-app Quick Look overlay for image/PDF attachments (#49)
- **web**: adaptive dark-mode rendering of received HTML email (#48)
- **web**: prefetch adjacent messages on j/k so Enter opens instantly (#46)
- **web**: sender/recipient avatars (initials + deterministic color) across Postbox (#45)
- **web**: layout-matching skeletons + designed empty states for Postbox (#44)
- **web**: undoable triage — "Archived — Undo (Cmd+Z)" toast for archive/trash/move/spam (#43)
- **web**: auto-advance to the next conversation after archive/trash/snooze (#42)
- **web**: Superhuman-parity single-key shortcuts + "?" cheat-sheet overlay (#41)
- **web**: Cmd+Enter send, Cmd+Shift+Enter send-later, Esc minimizes the composer (#40)
- **web**: actionable fix on the delivery cannot-send card (#39)
- **web**: warn before adding an unusable sending domain (freemail / no NS) (#38)
- **web**: stop guessing the Convex dashboard URL in the onboarding banner (#37)
- **api,web**: real reverse-DNS (PTR) preflight for the receiving MX host (#36)
- **web**: auto-detect the server public IP in the desktop setup wizard (#35)
- **web**: deep-link the exact app-password page on external-mailbox auth errors (#34)
- **web**: autodiscover IMAP/SMTP settings from the email address (#33)
- **web**: auto-recheck domain DNS verification so users need not click Verify repeatedly (#32)
- **web**: one-line domain readiness summary above the DNS records (#31)
- **web**: show why a DNS record failed to verify (found vs expected) (#30)
- suggest merged SPF record so adding DNS records doesn't break other mail providers (#25)
- **ai**: cross-source LLM-synthesized ask-anything answers with citations
- **inbox**: auto-archive inbound mail from blocklisted senders on receipt
- **files**: link auto-captured inbound attachments to the sender contact
- **setup-cli**: add `env --show` to list env vars the current flag state needs

### Changed
- **docs**: sans display prose + minimal content components (#175)
- **docs**: quiet chrome — header, sidebar, ToC, footer match the landing (#174)
- **marketing**: flat minimal pricing + statement CTA (no orbits, no tilt) (#173)
- **marketing**: TrustBar, OpenSource, Developer sections — one quiet rhythm (#172)
- **marketing**: feature overview without numbers/badges — quiet bento (#171)
- **marketing**: minimal hero — typography-led, calm product frame (#170)
- **marketing**: minimal Linear-style header + footer (drop beta bar) (#169)
- **ui**: migrate useRichText off execCommand to Selection/Range (native-undo bridge + full test matrix) (#92)
- **web**: extract UiAvatar and dedup chat avatar blocks (#29)
- **api**: extract shared email translation blob operations (#28)
- **api**: delete duplicate MTA client, route systemMail via sendProviderDispatch (#27)
- **api**: bake feature-flag floor into chat/assistant fn wrappers (#26)
- **api**: collapse hand-rolled permission ceremony onto requireOrgPermission (#23)
- **api**: use adminQuery wrapper for inline org:manage reads (#22)
- **api**: add getOrThrow load-or-404 helper (#21)
- **web**: extract UiSpinner and dedup 75 brand-spinner copies (#20)
- **web**: route local date/number formatters through formatters.ts (#19)
- **shared**: derive social-platform labels from one exhaustive Record (#18)

### Fixed
- **code-worker**: run untrusted agent+tests under a separate unprivileged UID (close /proc secret leak) (#145)
- **web,a11y**: set <html lang> (WCAG 3.1.1) (#143)
- **web,a11y**: correct mislabeled Close/Confirm icon buttons app-wide (#142)
- **build**: add dist/** to turbo build outputs (#137)
- **sdk-js**: cap the 429 Retry-After sleep (mirror the Java 30s cap) (#134)
- **code-worker**: resource limits, process-group kill, cap-drop, workspace cleanup (#133)
- **api**: stop returning the webhook HMAC secret to the client (#132)
- **api**: rate-limit the @assistant-in-chat path (LLM-cost DoS) (#131)
- **api**: audience dashboard uses denormalized counts (stop unbounded reactive collects) (#130)
- **api**: terminal status for retry-exhausted agentActions (retry cron no longer starves) (#128)
- **mail-sync**: deterministic synthetic Message-ID (stop backfill duplication) (#127)
- **api**: SES send is idempotent under dispatch retry (no double-delivery) (#125)
- **mta**: return the real message id on idempotency dedup (VERP attribution) (#123)
- **api**: re-check suppression list in the campaign worker before dispatch (#122)
- **web**: enforce composer attachment count/total-size limits (#120)
- **desktop**: stamp the release tag into the desktop version (auto-update resolves) (#119)
- **release**: single-target releases stop clobbering the shared "latest" (updater/install) (#118)
- **setup**: pin OWLAT_VERSION on the install pull path (deploy signed images, not :dev) (#117)
- **sdk**: handle empty-body 4xx/5xx as errors (was swallowed as success) (#115)
- **compose**: isolate untrusted code-worker on a dedicated network (#113)
- **web**: contact CSV export no longer throws past ~2,000 contacts (#112)
- **imap**: correct FETCH BODY literal octet count for non-ASCII/8-bit bodies (#111)
- **api**: enforce form-forced double opt-in at campaign send (#109)
- **api**: campaign send walker survives a thrown hop (retry/watchdog) (#108)
- **web**: block Postbox send while attachments are still uploading (#107)
- **web**: reopening a saved draft no longer overwrites its body with the signature (#105)
- **web**: email body iframe resizes to content (was clipped to 200px) (#104)
- **email-builder**: social block no longer silently dropped inside Container/Hero (#102)
- **email-builder**: string[] array editors no longer push objects (render crash) (#101)
- **email-scanner**: match unquoted hrefs + trim before scheme check (#99)
- **email-renderer**: safe JSON-in-HTML embedding (</script> breakout + control chars) (#98)
- **email-renderer**: escape color/font/width fields in style & VML (stored XSS) (#96)
- **setup**: make owlat quickstart work under Docker Desktop (#63)
- **desktop**: force exec bit on packed shell scripts for Windows clients (#59)
- **web,desktop**: use cross-env so the desktop build works on Windows (#56)
- **install**: guard install.sh to Linux with an actionable message (#55)
- **api**: single-`.paginate()`-safe audience/segment counting; bump convex-test to 0.0.54 (#24)
- **ai**: annotate ask authedAction with authz opt-out for cross-file gate
- **crypto**: pass an explicit GCM authTagLength
- **imap**: build the image with the @owlat/shared workspace package
- **docs**: use useRoute() instead of the template $route global
- **email-builder**: build the item-ref example in script, not inline

### Documentation
- **self-hosting**: MTA & ClamAV are profile-gated (stock install ships no AV) (#141)
- bring Postbox docs current with shipped email-experience features (#124)
- link to wolves.ink from the README header (#64)
- **self-hosting**: add supported-platforms OS matrix (#54)
- **contributing**: document Windows dev needs WSL2 or Git Bash (#52)
- **env**: reflect removal of inert NOTIFICATION_PROVIDER and VECTOR_STORE seams
- **knowledge-graph**: fix broken in-page anchor and script path
- **knowledge-graph**: reconcile roadmap and vision with shipped graph features
- ask-anything is now cross-source LLM-synthesized with citations
- **inbox**: blocklisted senders are auto-archived on inbound receipt
- **security**: scope campaign scan claim to default-language body
- **campaigns**: reflect that campaign sends are scanner-gated
- **files**: captured attachments now link to the sender contact
- document `owlat-setup env --show`
